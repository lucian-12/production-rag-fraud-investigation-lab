# The default fixture mode requires no database and no API key.
DEMO_STORAGE=fixture

# Docker Compose overrides DEMO_STORAGE and uses this PostgreSQL connection.
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag_demo
